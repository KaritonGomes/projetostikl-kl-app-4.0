import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.econe.kl',
  appName: 'KL RENT A CAR',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
